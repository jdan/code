import java.util.*;
/**
 * Write a description of class Rounded here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class Rounded
{
    public static void main() {
        int x_0;
        for (long i = (long) Math.pow(10,13); i <= (long) Math.pow(10,14); i++) {
            if (i % 2 ==0) {
               x_0 = 
        }
    }
}
